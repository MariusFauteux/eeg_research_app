"""Plot tabs shown in the session view's tab area.

Each module here is a ``QWidget`` that follows the ``PlotTab`` contract
documented at the top of ``ui/session_view.py`` (``update_plot`` + optional
``refresh_hz`` / ``set_channel_names``). Side-column controls live in
``ui/panels/`` instead.
"""
